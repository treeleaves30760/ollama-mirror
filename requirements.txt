# Core web framework
fastapi>=0.114.0,<0.120.0
uvicorn[standard]>=0.31.0,<0.35.0
requests>=2.31.0

# HTTP client for upstream requests - compatible with ollama 0.4.7
httpx>=0.27.0,<0.29.0

# Data validation and settings - compatible with ollama 0.4.7
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.0.0

# System monitoring and metrics
psutil>=7.0.0

# Environment configuration
python-dotenv>=1.1.0

# Optional: for development and testing
pytest>=8.4.0
pytest-asyncio>=1.1.0
aiofiles>=24.1.0

# Optional: for enhanced logging
structlog>=25.4.0

# Optional: for metrics export (Prometheus)
prometheus-client>=0.22.0

# Optional: for enhanced security
python-multipart>=0.0.20
python-jose[cryptography]>=3.5.0

# Optional: for web UI (if implemented)
jinja2>=3.1.6

# Type checking support
typing-extensions>=4.12.0
